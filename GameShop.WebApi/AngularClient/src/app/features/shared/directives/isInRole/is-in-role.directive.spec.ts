import { IsInRoleDirective } from './is-in-role.directive';

describe('IsInRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new IsInRoleDirective();
    expect(directive).toBeTruthy();
  });
});
